import { Types } from "./server.types";

import { store } from "../store";

export const openAccountActivationCodeDialog = async(dispatch, getState) => {
  // const response = await Promise.resolve();
  // dispatch({ type: Types., payload: response.todos })
};