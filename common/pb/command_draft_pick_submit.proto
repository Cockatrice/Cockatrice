syntax = "proto2";
import "game_commands.proto";

message Command_DraftPickSubmit {
    extend GameCommand {
        optional Command_DraftPickSubmit ext = 1036;
    }
    optional sint32 round = 1 [default = -1];
    optional bool overpicking = 2;
    optional bool returning = 3;
}
